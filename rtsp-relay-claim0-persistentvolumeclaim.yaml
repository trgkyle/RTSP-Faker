apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: horus
  labels:
    app: rtsp-relay-claim
  name: rtsp-relay-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
